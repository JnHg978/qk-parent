<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qk.management.mapper.BizTrackRecordMapper">
    <select id="selectByBizId" resultType="com.qk.vo.business.BizTrackRecordVO">
        select t1.id, t1.business_id, t1.user_id, t1.track_status,
               t1.key_items, t1.next_time, t1.record, t1.create_time,
               t2.name as assign_name
        from business_track_record t1
            left join user t2
                on t1.user_id = t2.id
        where t1.business_id = #{bizId}
    </select>
</mapper>